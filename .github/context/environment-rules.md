# Environment Rules

- Framework must support environment-driven API switching.
- No hardcoded URLs.
- Mock and real APIs must use centralized config management.
- Tests must remain environment-agnostic.
- Secrets must come from environment variables.
- Environment switching must require no test code changes.