# Mock Responses

Store versioned stub payloads used by the local mock server and contract-driven tests.

Suggested layout:
- `auth/` for authentication and identity flows
- `workflow/` for process and state transition responses
- `rbac/` for role and permission scenarios
- `notifications/` for email, SMS, and in-app notification payloads